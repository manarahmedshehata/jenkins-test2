pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                sh 'echo $currentBuild.currentResult';
                
            }
        }
        
    }
}
